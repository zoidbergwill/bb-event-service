load("@bazel_gazelle//:def.bzl", "gazelle")

# gazelle:prefix github.com/buildbarn/bb-event-service
# gazelle:go_naming_convention import
# gazelle:go_naming_convention_external import_alias
gazelle(
    name = "gazelle",
)

buildifier(
    name = "buildifier",
)
